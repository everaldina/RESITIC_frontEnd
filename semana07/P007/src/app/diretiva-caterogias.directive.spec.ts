import { DiretivaCaterogiasDirective } from './diretiva-caterogias.directive';

describe('DiretivaCaterogiasDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaCaterogiasDirective();
    expect(directive).toBeTruthy();
  });
});
