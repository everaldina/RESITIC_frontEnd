import { DiretivaPropriedadesDirective } from './diretiva-propriedades.directive';

describe('DiretivaPropriedadesDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivaPropriedadesDirective();
    expect(directive).toBeTruthy();
  });
});
