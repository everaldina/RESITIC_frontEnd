import { ForModificadoDirective } from './for-modificado.directive';

describe('ForModificadoDirective', () => {
  it('should create an instance', () => {
    const directive = new ForModificadoDirective();
    expect(directive).toBeTruthy();
  });
});
